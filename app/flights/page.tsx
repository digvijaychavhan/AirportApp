import { FlightInfo } from '@/components/flight-info'

export const metadata = {
  title: 'Flight Information — Airport Kiosk',
  description: 'Search for your flight details, gate, and boarding times.',
}

export default function FlightsPage() {
  return <FlightInfo />
}
